<IfModule mod_rewrite.c>
    RewriteEngine On

    # If your subdomain points to /public_html/presensi (project root),
    # forward all requests to /public internally.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

